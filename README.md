# 🤖 VK Stream Notify Bot v2

VK-бот уведомлений о стримах. Проверяет Twitch, YouTube, Kick, VK Play Live, Telegram-канал и группу ВК **без каких-либо прав от стримера** — только по публичным ссылкам.

---

## ✅ Что умеет бот

| Возможность | Описание |
|---|---|
| **Без доступа к группам** | Только публичные URL — стример ничего не даёт |
| **Умный парсинг постов** | ТГ и ВК: ищет ссылки на платформы + ключевые слова |
| **Мультистримерность** | Список стримеров, пользователь подписывается на нескольких |
| **VK кнопки** | Удобный выбор через интерактивную клавиатуру |
| **Проверка раз в минуту** | Уведомление только при начале стрима |

---

## 📁 Структура

```
vk_bot_v2/
├── bot.py          — главный файл (запускать его)
├── checker.py      — проверка платформ по публичным URL
├── database.py     — SQLite: подписки и состояния
├── config.py       — ВСЕ настройки
├── requirements.txt
└── bot.db          — БД (создаётся автоматически)
```

---

## ⚡ Быстрый старт (15 минут)

### Шаг 1 — Создай группу ВК для бота

1. vk.com → Сообщества → Создать сообщество
2. Тип: **Группа** (не паблик)
3. Управление → **Сообщения → Включить**

### Шаг 2 — Получи токен группы

1. Управление группой → **Работа с API → Ключи доступа**
2. Создать ключ, права: ✅ **Сообщения**, ✅ **Управление**
3. Скопируй токен

### Шаг 3 — Заполни config.py

```python
VK_TOKEN    = "vk1.a.xxxxxxx"      # токен из шага 2
VK_GROUP_ID = 123456789             # ID группы (из URL)

STREAMERS = [
    {
        "id":       "streamer1",         # латиница, без пробелов (внутренний ID)
        "name":     "Крутой Стример",    # имя в уведомлениях
        "twitch":   "https://www.twitch.tv/login",
        "youtube":  "https://www.youtube.com/channel/UCxxxx/live",
        "kick":     "https://kick.com/login",
        "vkplay":   "https://vkplay.live/login",
        "telegram": "https://t.me/channel",
        "vk_group": "https://vk.com/group",
    },
    # Добавь сюда ещё стримеров по шаблону
]
```

> Что не нужно — просто оставь `""`. Бот пропустит пустые поля.

### Шаг 4 — Установи зависимости

```bash
pip install -r requirements.txt
```

### Шаг 5 — Запусти

```bash
python bot.py
```

---

## 🎯 Как пользователь взаимодействует с ботом

1. Находит группу ВК → пишет любое сообщение или `/start`
2. Получает список стримеров с кнопками:
   ```
   ➕ Имя Стримера 1
   ➕ Имя Стримера 2
   📋 Мои подписки  |  ❌ Отписаться от всех
   ```
3. Нажимает на стримера → кнопка меняется на ✅, подписка активирована
4. Как только стример выходит в эфир → приходит уведомление

---

## 🔑 Ключи API (опционально, но рекомендуется)

### Twitch API — ОБЯЗАТЕЛЬНО для надёжной работы
Без ключей Cloudflare блокирует HTML-парсинг Twitch.

1. Зайди на https://dev.twitch.tv/console/apps
2. Register Your Application → имя любое, OAuth Redirect: `http://localhost`
3. Скопируй `Client ID` и `Client Secret` в config.py:
```python
TWITCH_CLIENT_ID     = "xxxxxx"
TWITCH_CLIENT_SECRET = "xxxxxx"
```
**Бесплатно. Не требует никаких прав от стримера.**

### YouTube API — рекомендуется
1. https://console.cloud.google.com → создай проект
2. APIs & Services → Enable APIs → **YouTube Data API v3**
3. Credentials → Create API Key → скопируй:
```python
YOUTUBE_API_KEY = "AIzaXXXXXX"
```
**Бесплатно (10 000 запросов/день — более чем достаточно).**

---

## 🧠 Как работает проверка постов в TG и ВК

Пост считается анонсом стрима если выполняется **хотя бы одно** условие:

**Условие A** — в посте есть ссылка на стрим-платформу:
- twitch.tv, youtube.com/watch, youtu.be, kick.com, vkplay.live

**Условие B** — в тексте поста встречаются **2+ ключевых слова** из списка:
- стрим, stream, live, лайв, эфир, трансляция, начал, стримим, в эфире, онлайн стрим, смотрите

Список слов и минимальное количество совпадений настраивается в `config.py`.

---

## ☁️ Хостинг на Bothost.ru

### Пошаговая инструкция

1. **Зарегистрируйся** на https://bothost.ru (бесплатно, без карты)

2. **Создай репозиторий GitHub** и загрузи туда папку с ботом:
   ```bash
   git init
   git add .
   git commit -m "vk bot"
   git branch -M main
   git remote add origin https://github.com/ВАШ_ЛОГИН/vk-bot.git
   git push -u origin main
   ```

3. **В Bothost**: Создать бота → тип Python → подключи репозиторий GitHub

4. **Добавь переменные окружения** в Bothost (вместо правки config.py):
   - Либо просто загрузи config.py с токеном через файловый менеджер Bothost

5. **Добавь файл** `Procfile` в корень:
   ```
   worker: python bot.py
   ```

6. **Запусти** — бот будет работать 24/7

### Тарифы Bothost.ru
| Тариф | Цена | RAM | Ботов |
|---|---|---|---|
| Бесплатный | 0₽ | 256 MB | 1 |
| Basic | 99₽/мес | 1 GB | 5 |
| Pro | 250₽/мес | 4 GB | 20 |

**Для одного бота хватит бесплатного тарифа** (256 MB RAM, до 50–100 подписчиков).

---

## ❓ Частые вопросы

**Бот не отвечает**
→ Проверь, включены ли «Сообщения» в настройках группы ВК

**Twitch всегда показывает offline**
→ Добавь Twitch API ключи — без них Cloudflare блокирует запросы

**Telegram не обнаруживает стрим**
→ Канал должен быть публичным. Проверь, что ссылка в формате `https://t.me/имя`

**Хочу добавить нового стримера**
→ Просто добавь новый блок в `STREAMERS` в config.py и перезапусти бота

**Можно ли подключить много стримеров?**
→ Да, добавляй сколько нужно. Бот проверяет всех параллельно по очереди.
